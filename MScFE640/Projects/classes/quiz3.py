import numpy as np
import pandas as pd

def par_to_spot(par_array):
    a = 100 # Assume that the default price of bond is 100
    indicator = 0

    for i in count(par_array):
        for j in range(0,i):
            if (i == 0):
                indicator +

